FROM node:20-alpine
WORKDIR /app

ENV NEXT_TELEMETRY_DISABLED=1

# Install deps first (cached layer)
COPY package.json package-lock.json ./
RUN npm ci

# Source is volume-mounted at runtime — copy provides an initial base
COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev"]
