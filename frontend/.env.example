# URL of the FastAPI backend, accessible from the browser
# For local development without Docker: http://localhost:8000
# For Docker Compose: http://localhost:8000 (backend port is exposed to host)
NEXT_PUBLIC_API_URL=http://localhost:8000
